<!DOCTYPE html>
<html lang="{{ language.code }}" dir="{{ theme.is_rtl ? 'rtl' : 'ltr' }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  {% hook 'head:start' %}
  {% hook head %}
  <link rel="stylesheet" href="{{ 'app.css' | asset }}">
  {% if theme.font %}<link rel="stylesheet" href="{{ theme.font.path|cdn }}">{% endif %}
  <link rel="stylesheet" href="{{ 'fonts/sallaicons.css'|cdn }}">
  <style>
    :root {
      --font-main: '{{ theme.font.name|default('Alexandria') }}';
      --baynora-navy:#2B2D42;
      --baynora-gray:#8D99AE;
      --baynora-gold:#D4AF37;
      --baynora-light:#EDF2F4;
      --baynora-white:#FFFFFF;
      --color-primary:#2B2D42;
    }
  </style>
  {% hook 'head:end' %}
</head>
<body id="app" class="baynora-theme {{ theme.settings.get('header_is_sticky', true) ? 'has-sticky-header' : '' }}">
  {% hook 'body:start' %}
  <div class="baynora-app">
    {% component 'header.header' %}
    <main id="main-content">{% block content %}{% endblock %}</main>
    {% component 'footer.footer' %}
  </div>
  {% hook 'body:end' %}
  <salla-search data-testid="store-search-modal"></salla-search>
  <salla-login-modal data-testid="store-login-modal"></salla-login-modal>
  <script defer src="{{ 'app.js' | asset }}"></script>
  {% block scripts %}{% endblock %}
</body>
</html>
